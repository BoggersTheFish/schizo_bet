Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

Tension propagates along directed edges in a causal neighborhood. Each layer applies learned pairwise gates that resemble structural stress between token positions. The stack does not use full-sequence softmax self-attention; it aggregates messages from a bounded past window only. Training minimizes cross-entropy next-byte prediction with auxiliary terms on MoE routing entropy and slow manifold drift. Byte tokenization maps each UTF-8 byte to an integer class. Replace this placeholder with your real corpus for serious runs; this file exists so the documented CLI example works out of the box.

